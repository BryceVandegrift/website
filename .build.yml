image: archlinux
oauth: pages.sr.ht/PAGES:RW
packages:
  - hut
environment:
  site: bpv.srht.site
tasks:
  - package: |
      cd $site
      tar -cvz . > ../site.tar.gz
  - upload: |
      hut pages publish -d $site site.tar.gz
